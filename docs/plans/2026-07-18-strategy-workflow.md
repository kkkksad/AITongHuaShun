# 策略实验室阶段式工作流实施计划

**状态：** 已完成代码与自动化验证；浏览器尺寸验收待后续环境验证

**目标：** 把策略页从纵向报告堆叠改为“配置 → 回测 → 独立验证 → Paper 观察”的任务流程，让用户始终知道当前结果来自哪里、下一步该做什么。

**架构：** `App.tsx` 继续持有策略参数与已提交参数，新增当前阶段和首次回测完成状态。`StrategyWorkflow` 负责阶段导航、来源说明和过期结果提示；重型研究组件仅在对应阶段挂载。纯函数模块负责参数差异与阶段状态，使用 Vitest 固定行为。

## 文件边界

- `src/lib/strategyWorkflow.ts`：阶段类型、参数一致性和阶段状态计算。
- `src/lib/strategyWorkflow.test.ts`：运行前、运行后和参数变更后的确定性测试。
- `src/components/StrategyWorkflow.tsx`：阶段导航、上下文摘要、过期提示和阶段容器。
- `src/App.tsx`：编排阶段、回测完成跳转和按阶段懒加载。
- `src/styles/index.css`：桌面与手机阶段导航。

## 实施任务

- [x] 以失败测试固定运行前、运行后和配置过期语义。
- [x] 实现策略工作流状态纯函数，聚焦测试 3 项通过。
- [x] 实现四阶段导航、来源边界和过期提示。
- [x] 按阶段挂载配置、合成回测、真实验证和 Paper 观察组件。
- [x] 运行完整前端、服务端测试、TypeScript 和生产构建。
- [ ] 验证 1440×900 与 390×844，无页面级横向溢出或控制台错误。
- [x] 更新当前状态文档。

## 自动化验证

- 策略工作流聚焦测试通过。
- 完整前端与服务端 Vitest 测试通过。
- TypeScript 项目检查和 Vite 生产构建通过。
- `git diff --check` 通过，仅有行尾转换警告。
