# 玄枢 Quant 全面用户体验优化计划

**状态：** P0 已完成，P1/P2 待后续迭代
**目标：** 降低首次进入和日常操作的认知负担，让用户能快速判断系统状态、进入正确工作流，并且在任何数据缺失或失败时知道当前还能信任什么。

## P0：本轮立即完成

- [x] **订单语义完整**：订单创建时持久化股票名称；旧订单使用当前行情或持仓名称补全；列表搜索和 CSV 导出同时支持名称与代码。
- [x] **真实总览**：移除固定日期、静态收益结论和“模拟行情”硬编码，改为真实连接状态、行情时间、paper 权益、现金、当日盈亏、持仓和挂单。
- [x] **工作流入口**：总览提供“市场研判 / 模拟下单 / 订单复核 / 策略验证”四个明确入口，不再让用户在页面堆叠中猜下一步。
- [x] **导航减负**：侧栏按“工作台 / 研究 / 交易与管理”分组；账户页按“资产与交易 / 研究与系统”分组，并为标签增加图标。
- [x] **可用搜索**：把无功能的顶部搜索改为快速跳转，支持总览、市场、策略、账户、研究管线和设置，提供键盘回车与 Esc。
- [x] **一致状态**：网络/5xx 短重试，认证和 4xx 不重试；主要研究模块刷新失败保留上次成功结果；总览、侧栏和研究环境读取同一份连接、行情源与 paper 执行事实。
- [x] **响应式验证**：检查 1440×900 和 390×844，无页面级横向滚动、文本遮挡或不可达操作；手机账户子导航改为固定网格，不再出现滚动箭头。

## P1：下一阶段

- [x] 把策略实验室拆成“配置策略 → 合成回测 → 独立真实验证 → Paper 观察”的阶段式工作流；结果过期会提示重跑，重型内容按阶段挂载。
- [ ] 为市场、策略、账户和研究管线建立统一的页面状态组件：实时、缓存、降级、离线、更新时间、重试入口。
  - 2026-07-18 最小切片：新增统一 `ResearchQueryState`，固定首次加载、无缓存阻断失败和保留缓存刷新失败三种语义；港股观察已接入，其余研究模块留待后续独立迁移。
  - 2026-07-18 最小切片：期货研判已迁移到统一 `ResearchQueryState`，首次加载、无缓存失败和保留缓存刷新失败不再由页面重复实现；其余研究模块留待后续独立迁移。
  - 2026-07-18 最小切片：A 股策略多窗口稳健性面板已迁移到统一 `ResearchQueryState`，保留既有真实历史报告与刷新降级语义；其余研究模块留待后续独立迁移。
  - 2026-07-18 最小切片：板块展望与形态识别面板已迁移到统一 `ResearchQueryState`，保留既有真实历史报告、首次失败和缓存刷新失败语义；其余研究模块留待后续独立迁移。
  - 2026-07-18 最小切片：A 股五日变盘雷达已迁移到统一 `ResearchQueryState`，统一首次加载、无缓存失败和保留缓存刷新失败语义；其余研究模块留待后续独立迁移。
  - 2026-07-18 最小切片：新股申购研究已迁移到统一 `ResearchQueryState`，移除页面内重复的加载、阻断失败和缓存刷新失败实现；其余研究模块留待后续独立迁移。
  - 2026-07-18 最小切片：个股趋势研判已迁移到统一 `ResearchQueryState`，保留按名称/代码查询、具体错误提示和缓存刷新降级语义；其余研究模块留待后续独立迁移。
  - 2026-07-18 最小切片：真实新闻与外围市场面板已迁移到统一 `ResearchQueryState`，保留真实只读事件流、上游数据质量警告和缓存刷新降级语义；其余研究模块留待后续独立迁移。
  - 2026-07-18 最小切片：A 股概览的板块 5 日展望已迁移到统一 `ResearchQueryState`，刷新失败时保留真实历史评分并显示最后成功更新时间；其余页面状态留待后续独立迁移。
- [ ] 为所有长列表增加服务端分页或虚拟化；按页面统计首屏接口数、数据量和交互就绪时间。
- [ ] 将研究运行记录、参数版本、样本范围、成本模型和样本外结果统一成可追溯实验详情页。
- [ ] 增加每日任务中心：盘前检查、盘中观察、盘后复盘、异常处理和未完成项。

## P2：架构、数据与运营

- [ ] PostgreSQL 保存实验元数据、订单审计与通知结果；行情缓存继续保留有界 TTL、容量和来源版本。
- [ ] 建立 point-in-time 股票池、purged walk-forward、概率校准、交易成本压力测试和策略退化告警。
- [ ] 统一 OpenTelemetry/Prometheus 指标，覆盖接口延迟、外部源成功率、缓存命中率、研究耗时和通知送达率。
- [ ] 云部署采用 HTTPS、密钥服务、每日备份、日志轮转和健康告警；真实交易继续保持关闭。

## 验证门槛

1. 所有新增数据字段向后兼容已有 JSON 订单。
2. `npm test`、`npm run build` 与 AkShare 桥测试通过。
3. 浏览器桌面和手机视口检查通过，控制台无新增错误。
4. 文档明确区分真实只读行情、历史条件频率、确定性模拟回测和本地 paper 账户。

## 本轮验证记录

- 聚焦测试：3 个文件、43 项通过。
- 快速跳转补充页面名称与工作流关键词匹配，并以纯函数测试覆盖六类页面、回车首项选择和 Esc 关闭；8 项测试通过。
- 完整服务端测试：40 个文件、691 项通过。
- 完整前端测试：9 个文件、39 项通过。
- AkShare 桥测试：84 项通过，保留 1 条第三方 `httpx` 弃用警告。
- 生产构建通过，转换 2,306 个模块。
- 浏览器验证：快捷跳转、四个总览工作流、分组账户导航和订单名称通过；1440×900 与 390×844 均无页面级横向溢出，最终干净页面控制台无 `error` 或 `warn`。
- `git diff --check` 通过，仅有 Windows 行尾转换警告。
