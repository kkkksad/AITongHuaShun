# AGENTS.md

本文件是 KAIROS Quant 的导航地图，不是完整手册。进入任务后，只读取与任务有关的文档，并把长期有效的信息更新到对应的 `docs/` 文件。

## 项目目标

KAIROS Quant 是一个面向量化研究、确定性回测和模拟交易的渐进式工作台。

当前仓库已恢复为可运行的 React + TypeScript 原型。所有市场、新闻、账户和回测数据仍然是明确标注的本地模拟数据。

## 首先阅读

- 当前事实与已知缺口：`docs/status/current-state.md`
- 产品能力与非目标：`docs/product/overview.md`
- 目标架构与模块边界：`docs/architecture/system-overview.md`
- 安装、运行和检查命令：`docs/operations/development.md`
- 交易、数据和权限红线：`docs/safety/trading-boundaries.md`
- 接入顺序与下一阶段：`docs/roadmap.md`

完整目录见 `docs/README.md`。

## 按任务导航

- 修改页面或交互：先读 `docs/product/overview.md` 和 `docs/architecture/system-overview.md`。
- 修改回测、指标或策略：先确认确定性、数据时间边界和成本模型。
- 接入行情或新闻：先读 `docs/safety/trading-boundaries.md`，保留来源、时间和授权信息。
- 接入账户或订单：默认只允许模拟交易；任何真实执行能力必须先建立独立权限域和人工批准。
- 开始较大工作：在 `docs/plans/` 创建执行计划并持续记录进度、决策和验证结果。
- 改变架构或长期约束：在 `docs/decisions/` 新增决策记录，不要只写在聊天或提交信息里。

## 不可违反的约束

- 不得把模拟数据或回测结果描述为真实收益。
- 不得直接连接未经审计的自动交易服务与真实券商账户。
- 行情读取和订单执行必须属于不同权限域。
- 下单能力必须具备账户白名单、额度限制、风险检查、幂等键、审计日志和逐笔人工批准。
- 不得引入未来数据、时间穿越或无法复现的随机回测结果。
- 不得虚构当前仓库中不存在的源码、测试、接口或运行结果。

## 开发流程

1. 阅读当前状态和与任务直接相关的专题文档。
2. 核对代码、配置和文档是否一致。
3. 先写可验证的计划或测试，再实施最小改动。
4. 运行与改动范围匹配的测试和构建检查。
5. 更新受影响的专题文档、状态页或决策记录。

标准命令：

```powershell
npm install
npm run dev -- --host 127.0.0.1
npm test
npm run build
```

最近一次测试和构建结果见 `docs/status/current-state.md`。

## 文档维护规则

- `AGENTS.md` 只保存地图、硬约束和通用工作流，目标控制在约 100 行以内。
- 一个主题只保留一个权威文档，其他位置使用链接，不复制整段内容。
- 明确区分“当前已实现”“目标设计”“计划中”和“未知”。
- 代码或配置改变事实时，在同一项工作中更新文档。
- 计划完成后保留验证结果；过期计划应标明状态，不应悄悄删除历史决策。
