# AI量化 智能量化交易系统

## 核心规则
- 每次代码修改完成后，必须运行匹配范围的测试和构建检查。
- Git 提交标题和正文全部使用中文，不使用 `feat`、`fix`、`chore` 等英文前缀。
- 每轮只完成一个可验证的最小任务，提交并推送后结束本轮。
- 当前只允许研究、回测和模拟交易，不得接入真实券商下单。
- 真实交易只能在权限、审批、风控、审计和紧急停止能力独立完成后单独立项。

## 开发流程
1. 先 git pull 拉取最新代码
2. 查看当前项目状态和最近的提交记录
3. 选择当前最重要的功能或修复
4. 实现功能，写测试和文档
5. 使用中文提交信息执行 `git add -A && git commit && git push`
6. 结束本轮，等待下一次调度
