# AI量化 智能量化交易系统

## 核心规则
- 每次代码修改完成后，**必须执行** `git add -A && git commit -m "描述你的改动" && git push`
- 不要停下来 — 完成一个任务后立刻开始下一个
- 最终目标：达到可实盘交易状态

## 开发流程
1. 先 git pull 拉取最新代码
2. 查看当前项目状态和最近的提交记录
3. 选择当前最重要的功能或修复
4. 实现功能，写测试和文档
5. git add -A && git commit && git push
6. 继续下一个任务
